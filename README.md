Para utilizar la carpeta PJ en modo select, adicionar esta linea en Preferences > Key Bindings
```
{ "keys": ["ctrl+f1"], "command": "show_overlay", "args": {"overlay": "command_palette", "text": "Snippet: str_re"}}
```
y usar ctrl + f1.
